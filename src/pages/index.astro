---
// pages/index.astro — Página de inicio (Home)
import Layout from '../layouts/Layout.astro';
import Hero from '../components/home/Hero.astro';
import SobrePablo from '../components/home/SobrePablo.astro';
import AudienceCards from '../components/home/AudienceCards.astro';
import PruebaSocial from '../components/home/PruebaSocial.astro';
import EventosDestacados from '../components/home/EventosDestacados.astro';
import VideosPreview from '../components/home/VideosPreview.astro';
import GaleriaCongresos from '../components/conferencias/GaleriaCongresos.astro';
import Button from '../components/ui/Button.astro';
---

<Layout
  title="Pablo Borrás — Médico Veterinario, Speaker y Autor | Buenos Aires"
  description="Pablo Borrás, Médico Veterinario MSc especialista en enfermedades infecciosas y parasitarias de perros y gatos. Speaker internacional, autor del Manual Práctico y docente veterinario. Buenos Aires, Argentina."
>

  <!-- 1. Hero principal con 3 CTAs por audiencia -->
  <Hero />

  <!-- 2. Tres caminos: Tutores / Veterinarios / Empresas -->
  <AudienceCards />

  <!-- 3. Bloque "Sobre Pablo" — bio + stats de autoridad -->
  <SobrePablo />

  <!-- 4. Strip de prueba social con números de autoridad -->
  <PruebaSocial />

  <!-- 3. Próximos 2 eventos destacados -->
  <EventosDestacados />

  <!-- 4. Vista previa de 3 videos -->
  <VideosPreview limit={3} />

  <!-- 4b. En los medios -->
  <section style="background: var(--color-neutral-light);" class="py-14 md:py-16">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">

      <div class="text-center mb-10 reveal">
        <p class="text-xs font-semibold uppercase tracking-widest mb-2" style="color: var(--color-accent);">Prensa</p>
        <h2 class="text-3xl font-bold" style="font-family: var(--font-display); color: var(--color-text);">En los medios</h2>
        <p class="text-sm mt-3 max-w-xl mx-auto" style="color: var(--color-text-muted);">Apariciones en medios nacionales e internacionales de Argentina.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
        {[
          {
            medio: 'Infobae',
            tipo: 'Nota periodística',
            titulo: 'El 75% de los dueños de mascotas pide mejores recomendaciones para evitar parásitos',
            año: '2026',
            href: 'https://www.infobae.com/perros-y-gatos/2026/03/20/el-75-de-los-duenos-de-mascotas-pide-mejores-recomendaciones-para-evitar-parasitos/',
            cta: 'Leer nota →',
            accentColor: '#DC2626',
            accentBg: 'rgba(220,38,38,0.07)',
            accentBorder: 'rgba(220,38,38,0.2)',
            logoBg: '#ffffff',
            logoText: 'in',
            logoImg: '/imagenes/logo/infobaelogo.webp',
          },
          {
            medio: 'La Nación',
            tipo: 'Video viral · TikTok',
            titulo: 'Consejo de Pablo sobre prevención de parásitos elegido por La Nación',
            año: '2025',
            href: 'https://www.tiktok.com/@lanacioncom/video/7578953045355072824',
            cta: 'Ver video →',
            accentColor: 'var(--color-primary)',
            accentBg: 'rgba(27,62,75,0.07)',
            accentBorder: 'rgba(27,62,75,0.2)',
            logoBg: '#ffffff',
            logoText: 'LN',
            logoImg: '/imagenes/logo/lanacion.webp',
          },
          {
            medio: 'Mi Negocio Veterinario',
            tipo: 'Entrevista · Podcast',
            titulo: 'De las redes a la cumbre de la industria veterinaria',
            año: '2022',
            href: 'https://www.minegocioveterinario.com/2022/11/de-las-redes-a-la-cumbre-de-la-industria-veterinaria/',
            cta: 'Leer entrevista →',
            accentColor: 'var(--color-accent)',
            accentBg: 'rgba(24,128,201,0.07)',
            accentBorder: 'rgba(24,128,201,0.20)',
            logoBg: 'var(--color-accent)',
            logoText: 'MNV',
            logoImg: '/imagenes/logo/logominegocio.webp',
          },
          {
            medio: 'Instagram viral',
            tipo: 'Reel · Contenido propio',
            titulo: 'Reel sobre prevención de parásitos que se volvió viral entre tutores de mascotas',
            año: '2024',
            href: 'https://www.instagram.com/reel/C_lp1A9PlAg/',
            cta: 'Ver reel →',
            accentColor: '#E4405F',
            accentBg: 'rgba(228,64,95,0.07)',
            accentBorder: 'rgba(228,64,95,0.2)',
            logoBg: '#ffffff',
            logoText: 'IG',
            logoImg: '/imagenes/logo/infobaelogo.webp',
          },
        ].map((item, i) => (
          <a
            href={item.href}
            target="_blank"
            rel="noopener noreferrer"
            class="reveal media-card flex items-start gap-5 p-6 bg-white"
            style={`border-radius: var(--radius); box-shadow: var(--shadow); text-decoration: none; transition-delay: ${i * 0.1}s; border-left: 3px solid ${item.accentColor}; transition: transform 0.25s ease, box-shadow 0.25s ease, border-left-width 0.2s ease;`}
          >
            <div
              class="shrink-0 w-12 h-12 rounded-xl flex items-center justify-center text-white text-xs font-bold overflow-hidden"
              style={`background: ${item.logoBg}; border: 1px solid rgba(0,0,0,0.08);`}
            >
              {item.logoImg
                ? <img src={item.logoImg} alt={item.medio} style="width: 100%; height: 100%; object-fit: contain; padding: 6px;" loading="lazy" />
                : item.logoText
              }
            </div>
            <div class="flex-1 min-w-0">
              <div class="flex items-center gap-2 mb-1 flex-wrap">
                <span class="text-xs font-bold" style={`color: ${item.accentColor};`}>{item.medio}</span>
                <span class="text-xs px-2 py-0.5 rounded-full" style={`background: ${item.accentBg}; color: ${item.accentColor}; border: 1px solid ${item.accentBorder};`}>{item.tipo}</span>
              </div>
              <p class="text-sm leading-snug font-semibold mb-3" style="color: var(--color-text);">{item.titulo}</p>
              <div class="flex items-center justify-between">
                <span class="text-xs" style="color: var(--color-text-muted);">{item.año}</span>
                <span
                  class="media-cta inline-flex items-center gap-1 text-xs font-semibold"
                  style={`color: ${item.accentColor}; opacity: 0.72; transition: opacity 0.2s;`}
                >
                  {item.cta.replace(' →', '')}
                  <svg
                    class="media-arrow"
                    style="transition: transform 0.2s ease; flex-shrink: 0;"
                    width="13"
                    height="13"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2.5"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    aria-hidden="true"
                  >
                    <line x1="5" y1="12" x2="19" y2="12"/>
                    <polyline points="12 5 19 12 12 19"/>
                  </svg>
                </span>
              </div>
            </div>
          </a>
        ))}
      </div>

    </div>
  </section>

  <!-- 4c. Galería de congresos internacionales -->
  <GaleriaCongresos />

  <!-- 5. Teaser del libro técnico (solo para veterinarios) -->
  <section class="py-14 md:py-16 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div
        class="flex flex-col md:flex-row items-center gap-6 md:gap-8 p-6 md:p-8 rounded-2xl text-center md:text-left"
        style="background: linear-gradient(135deg, var(--color-neutral-light) 0%, #fff 100%); border-left: 4px solid var(--color-accent); box-shadow: var(--shadow);"
      >
        <!-- TODO: Pablo debe confirmar el nombre del archivo de imagen de tapa -->
        <div class="relative shrink-0">
          <img
            src="/imagenes/libro/tapa-libro.webp"
            alt="Manual Práctico de Enfermedades Infecciosas y Parasitarias en el Perro"
            class="w-24 rounded-lg object-cover"
            style="box-shadow: var(--shadow);"
            onerror="this.style.display='none'; this.nextElementSibling.style.display='flex'"
          />
          <div style="display:none; width:96px; height:128px; background:var(--color-primary); border-radius:var(--radius); align-items:center; justify-content:center; color:white; font-size:11px; text-align:center; padding:8px; flex-direction:column; gap:4px;">
            📚<span>Manual<br/>Práctico</span>
          </div>
        </div>
        <div class="flex-1">
          <p class="text-xs font-semibold uppercase tracking-widest mb-1" style="color: var(--color-accent);">
            Exclusivo para profesionales
          </p>
          <h2
            class="text-xl font-bold mb-2"
            style="font-family: var(--font-display); color: var(--color-text);"
          >
            ¿Sos veterinario? Conocé el libro técnico.
          </h2>
          <p class="text-sm" style="color: var(--color-text-muted);">
            Manual Práctico de Enfermedades Infecciosas y Parasitarias en el Perro — Pablo Borrás · Disponible en Multimedica Ediciones, Mercado Libre y Amazon.
          </p>
        </div>
        <div class="shrink-0 w-full md:w-auto">
          <Button href="/veterinarios/libro" variant="primary" size="md" class="w-full md:w-auto">
            Ver el libro →
          </Button>
        </div>
      </div>
    </div>
  </section>

  <!-- 6. CTA final de contacto general -->
  <section
    class="py-16 md:py-20 text-center"
    style="background: var(--color-secondary);"
  >
    <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2
        class="text-3xl md:text-4xl font-bold text-white mb-4"
        style="font-family: var(--font-display);"
      >
        ¿Hablamos?
      </h2>
      <p class="text-white/70 mb-8 text-lg">
        Ya sea que quieras una interconsulta, contratar una charla o colaborar en un proyecto, estoy disponible para atenderte.
      </p>
      <Button href="/contacto" variant="primary" size="lg">
        Ir al formulario de contacto
      </Button>
    </div>
  </section>

</Layout>

<style>
  /* En los medios — hover lift en cards */
  .media-card {
    transition: transform 0.25s ease, box-shadow 0.25s ease, border-left-width 0.2s ease;
  }
  .media-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 16px 40px rgba(0, 0, 0, 0.10) !important;
    border-left-width: 5px !important;
  }

  /* Flecha se mueve al hover */
  .media-card:hover .media-arrow {
    transform: translateX(4px);
  }

  /* CTA sube a opacity completa al hover */
  .media-card:hover .media-cta {
    opacity: 1 !important;
  }
</style>
